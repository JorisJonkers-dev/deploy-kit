# GENERATED. Never hand-edit.
apiVersion: v1
kind: ConfigMap
metadata:
  name: gatus-endpoints
  namespace: utility-system
  labels:
    app.kubernetes.io/managed-by: deploy-kit
data:
  config.yaml: "endpoints:\n  # from the `management` exposure -- authored host, plus its one route\n\
    \  #    {path: /, match: prefix, workload: rabbitmq, surface: management}\n  #    + probes.readiness\
    \ {tcp: 5672}   <- different port, different protocol\n  - name: rabbitmq\n    group: data       \
    \              # the domain\n    url: https://rabbitmq.jorisjonkers.dev/    # host + route path, both\n\
    \                                               # authored on the exposure.\n                    \
    \                           # What is still missing is\n                                         \
    \      # not the URL but what a\n                                               # healthy answer to\
    \ it looks\n                                               # like -- see below\n    # NO `conditions:`.\
    \ The readiness declaration this entry is supposed to\n    # derive from is a TCP probe on a port\
    \ the edge does not serve, and the\n    # audience guarantees a non-200 for any prober without a session.\
    \ A Gatus\n    # endpoint with no conditions is not valid configuration and will not\n    # load --\
    \ which is the finding, not a formatting choice. Writing\n    # `[STATUS] == 200` here would be inventing\
    \ the answer to G-34.\n    #\n    # NO `interval` either: no field declares one and no chapter names\
    \ a\n    # default.\n"
