# GENERATED. Never hand-edit.
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: rabbitmq
  namespace: data-system
  labels:
    app.kubernetes.io/name: rabbitmq
    app.kubernetes.io/instance: rabbitmq
    app.kubernetes.io/part-of: platform-rabbitmq
    app.kubernetes.io/managed-by: deploy-kit
    app.kubernetes.io/component: none
spec:
  replicas: 1
  strategy:
    type: Recreate
  progressDeadlineSeconds: 360
  selector:
    matchLabels:
      app.kubernetes.io/name: rabbitmq
      app.kubernetes.io/instance: rabbitmq
  template:
    metadata:
      labels:
        app.kubernetes.io/name: rabbitmq
        app.kubernetes.io/instance: rabbitmq
        app.kubernetes.io/part-of: platform-rabbitmq
        app.kubernetes.io/managed-by: deploy-kit
        app.kubernetes.io/component: none
    spec:
      serviceAccountName: rabbitmq
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
        runAsUser: 999
        runAsGroup: 999
        fsGroup: 999
        fsGroupChangePolicy: OnRootMismatch
      containers:
      - name: rabbitmq
        image: docker.io/library/rabbitmq@sha256:902448b00f3d3f117daac08865d2f40971e33bd2fa8a863d1a23da3b83fd4f00
        ports:
        - name: amqp
          containerPort: 5672
        - name: management
          containerPort: 15672
        - name: metrics
          containerPort: 15692
        resources:
          requests:
            memory: 2Gi
            cpu: 500m
          limits:
            memory: 2Gi
        securityContext:
          readOnlyRootFilesystem: true
          capabilities:
            drop:
            - ALL
        startupProbe:
          tcpSocket:
            port: 5672
          periodSeconds: 5
          timeoutSeconds: 5
          failureThreshold: 24
        readinessProbe:
          tcpSocket:
            port: amqp
          timeoutSeconds: 5
          periodSeconds: 10
          failureThreshold: 3
          initialDelaySeconds: 0
        livenessProbe:
          tcpSocket:
            port: amqp
          timeoutSeconds: 5
          periodSeconds: 10
          failureThreshold: 3
          initialDelaySeconds: 0
        volumeMounts:
        - name: rabbitmq-data
          mountPath: /var/lib/rabbitmq
      volumes:
      - name: rabbitmq-data
        persistentVolumeClaim:
          claimName: rabbitmq-data
      automountServiceAccountToken: false
---
apiVersion: v1
kind: Service
metadata:
  name: rabbitmq
  namespace: data-system
  labels:
    app.kubernetes.io/name: rabbitmq
    app.kubernetes.io/instance: rabbitmq
    app.kubernetes.io/part-of: platform-rabbitmq
    app.kubernetes.io/managed-by: deploy-kit
    app.kubernetes.io/component: none
spec:
  selector:
    app.kubernetes.io/name: rabbitmq
    app.kubernetes.io/instance: rabbitmq
  ports:
  - name: amqp
    port: 5672
    targetPort: amqp
    protocol: TCP
  - name: management
    port: 15672
    targetPort: management
    protocol: TCP
  - name: metrics
    port: 15692
    targetPort: metrics
    protocol: TCP
