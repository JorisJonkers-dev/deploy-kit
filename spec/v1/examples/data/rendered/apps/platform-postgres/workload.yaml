# GENERATED. Never hand-edit.
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres
  namespace: data-system
  labels:
    app.kubernetes.io/name: postgres
    app.kubernetes.io/instance: postgres
    app.kubernetes.io/part-of: platform-postgres
    app.kubernetes.io/managed-by: deploy-kit
    app.kubernetes.io/component: none
spec:
  replicas: 1
  strategy:
    type: Recreate
  progressDeadlineSeconds: 180
  selector:
    matchLabels:
      app.kubernetes.io/name: postgres
      app.kubernetes.io/instance: postgres
  template:
    metadata:
      labels:
        app.kubernetes.io/name: postgres
        app.kubernetes.io/instance: postgres
        app.kubernetes.io/part-of: platform-postgres
        app.kubernetes.io/managed-by: deploy-kit
        app.kubernetes.io/component: none
    spec:
      serviceAccountName: postgres
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
            - matchExpressions:
              - key: kubernetes.io/arch
                operator: In
                values:
                - amd64
              - key: kubernetes.io/hostname
                operator: In
                values:
                - enschede-t1000-1
                - enschede-rx7900xtx-1
                - enschede-gtx-960m-1
                - frankfurt-contabo-1
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
        runAsUser: 999
        runAsGroup: 999
        fsGroup: 999
        fsGroupChangePolicy: OnRootMismatch
      containers:
      - name: postgres
        image: docker.io/pgvector/pgvector@sha256:1d757171e8712971926e2f2cc82508e1bc045578cabc2fb7bc134d839e5668b5
        ports:
        - name: postgres
          containerPort: 5432
        resources:
          requests:
            memory: 2Gi
            cpu: 500m
          limits:
            memory: 2Gi
        securityContext:
          readOnlyRootFilesystem: true
          capabilities:
            drop:
            - ALL
        env:
        - name: POSTGRES_DB
          value: postgres
        - name: POSTGRES_USER
          value: postgres
        envFrom:
        - secretRef:
            name: platform-postgres-exporter
        startupProbe:
          tcpSocket:
            port: 5432
          periodSeconds: 5
          timeoutSeconds: 5
          failureThreshold: 12
        readinessProbe:
          tcpSocket:
            port: postgres
          timeoutSeconds: 5
          periodSeconds: 10
          failureThreshold: 3
          initialDelaySeconds: 0
        livenessProbe:
          tcpSocket:
            port: postgres
          timeoutSeconds: 5
          periodSeconds: 10
          failureThreshold: 3
          initialDelaySeconds: 0
        volumeMounts:
        - name: postgres-data
          mountPath: /var/lib/postgresql/data
        - name: postgresql-conf
          mountPath: /etc/postgresql/postgresql.conf
          subPath: postgresql.conf
          readOnly: true
        - name: writable-var-run-postgresql
          mountPath: /var/run/postgresql
        - name: writable-tmp
          mountPath: /tmp
      - name: postgres-exporter
        image: quay.io/prometheuscommunity/postgres-exporter@sha256:06034c5595aef513e41b4d828317cfb61f44d0c1cee1533e5463bd69e42910d1
        ports:
        - name: metrics
          containerPort: 9187
        resources:
          requests:
            cpu: 10m
            memory: 64Mi
          limits:
            memory: 64Mi
        securityContext:
          readOnlyRootFilesystem: true
          capabilities:
            drop:
            - ALL
        envFrom:
        - secretRef:
            name: platform-postgres-exporter
      volumes:
      - name: postgres-data
        persistentVolumeClaim:
          claimName: postgres-data
      - name: postgresql-conf
        configMap:
          name: postgres-postgresql-conf-<content-hash>
          items:
          - key: postgresql.conf
            path: postgresql.conf
      - name: writable-var-run-postgresql
        emptyDir:
          sizeLimit: 64Mi
      - name: writable-tmp
        emptyDir:
          sizeLimit: 64Mi
      automountServiceAccountToken: false
---
apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: data-system
  labels:
    app.kubernetes.io/name: postgres
    app.kubernetes.io/instance: postgres
    app.kubernetes.io/part-of: platform-postgres
    app.kubernetes.io/managed-by: deploy-kit
    app.kubernetes.io/component: none
spec:
  selector:
    app.kubernetes.io/name: postgres
    app.kubernetes.io/instance: postgres
  ports:
  - name: postgres
    port: 5432
    targetPort: postgres
    protocol: TCP
  - name: metrics
    port: 9187
    targetPort: metrics
    protocol: TCP
