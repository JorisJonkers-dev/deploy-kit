<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>dev.jorisjonkers.deploykit.emf</groupId>
    <artifactId>emf-parent</artifactId>
    <version>0.1.0-SNAPSHOT</version>
  </parent>

  <artifactId>dev.jorisjonkers.deploykit.emf.parity</artifactId>
  <!--
    A plain jar: the suites decide every case from the files a run leaves behind, so the module holds
    no EMF type and resolves nothing from the target platform (docs/adr/emf/0120). It carries no
    META-INF/MANIFEST.MF, so the parent's `bundle` profile does not activate and its test libraries
    are declared here instead.
  -->
  <packaging>jar</packaging>
  <name>deploy-kit model-driven parity</name>

  <dependencies>
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.assertj</groupId>
      <artifactId>assertj-core</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>com.tngtech.archunit</groupId>
      <artifactId>archunit-junit5</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>
</project>
